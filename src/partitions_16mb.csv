# ESP32-S3 16 MB flash layout for the macro pad.
#
# Two OTA firmware slots and one FAT filesystem holding everything: the web
# UI, config.json and GIF uploads.
#
# FAT because USB mass storage exposes raw sectors that the host mounts with
# its own driver, and hosts only speak FAT. The wear levelling layer FFat
# mounts through presents logical sectors, which is exactly what the MSC
# read/write callbacks hand to the host.
#
# Note that `pio run -t uploadfs` cannot populate this partition: mkfatfs
# writes a raw FAT volume that does not survive wear levelling. Put files on
# it over USB mass storage, or with POST /api/fs/upload.
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xE000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x3F0000,
app1,     app,  ota_1,   0x400000, 0x3F0000,
storage,  data, fat,     0x7F0000, 0x800000,
